// The go directive is the language baseline; toolchain selects the exact patch
// used by setup-go, local workspace commands, releases, and Docker builders.
go 1.27.0

toolchain go1.27.1

use (
	.
	./e2e
	./e2e/infra
	./mockrelay
)
